# Slim runtime requirements for the FastAPI chatbot server (Render deploy).
# Keep this in sync with the runtime imports under ub_cse_bot/.
# For dev / re-crawling / evals, install the full requirements.txt instead.

# ---- Core ----
python-dotenv>=1.0.1
pydantic>=2.7
pydantic-settings>=2.3
tenacity>=8.5
httpx>=0.27
orjson>=3.10

# ---- Web server ----
fastapi>=0.111
uvicorn[standard]>=0.30

# ---- LLM (Vertex Gemini via google-genai) ----
google-cloud-aiplatform>=1.67
vertexai>=1.67
google-genai>=1.0

# ---- Retrieval ----
sentence-transformers>=3.0    # cross-encoder reranker fallback (small ms-marco-MiniLM)
rank-bm25>=0.2.2
pinecone>=5.0                 # renamed from `pinecone-client`; old name now errors at runtime
neo4j>=5.22

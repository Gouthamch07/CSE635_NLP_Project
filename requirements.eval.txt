# Extra dependencies needed only for the evaluation pipeline (scripts/evaluate_*.py).
# Install on top of either requirements.txt or requirements.runtime.txt.

# Graphs
matplotlib>=3.8

# Optional: real RAGAS scoring. Heavy deps + needs a judge LLM.
# `langchain-google-vertexai` lets RAGAS reuse our Vertex Gemini creds so no
# OpenAI key is required. evaluate_ragas.py auto-detects this path when
# OPENAI_API_KEY is unset.
ragas>=0.2
datasets>=2.20
pandas>=2.2
langchain-google-vertexai>=2.0
